# VINTF FCM level 7 stubs for kernel 5.4
# These configs are required =y by VINTF ARM64 conditional block
# but don't exist as Kconfig options in kernel 5.4

config HAVE_MOVE_PMD
	def_bool y

config HAVE_MOVE_PUD
	def_bool y

config KFENCE
	def_bool y
